///
/// HybridInappUpdatesSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `UpdateResult` to properly resolve imports.
namespace margelo::nitro::checkupdates { struct UpdateResult; }
// Forward declaration of `UpdateMode` to properly resolve imports.
namespace margelo::nitro::checkupdates { enum class UpdateMode; }

#include "UpdateResult.hpp"
#include <NitroModules/Promise.hpp>
#include "UpdateMode.hpp"
#include <functional>

namespace margelo::nitro::checkupdates {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `InappUpdates`
   * Inherit this class to create instances of `HybridInappUpdatesSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridInappUpdates: public HybridInappUpdatesSpec {
   * public:
   *   HybridInappUpdates(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridInappUpdatesSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridInappUpdatesSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridInappUpdatesSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<UpdateResult>> checkForUpdate() = 0;
      virtual std::shared_ptr<Promise<bool>> startUpdate(UpdateMode mode) = 0;
      virtual std::shared_ptr<Promise<bool>> completeUpdate() = 0;
      virtual void onProgress(const std::function<void(double /* percent */)>& progress) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "InappUpdates";
  };

} // namespace margelo::nitro::checkupdates
